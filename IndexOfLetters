using System;

namespace _09.IndexOfLetters2
{
    class Program
    {
        static void Main(string[] args)
        {            
            string word = Console.ReadLine();
          
            foreach (var letter in word)
            {
                int index = letter - 97;
                Console.WriteLine($"{letter} -> {index} ");
            }
        }       
    }   
}
